# Placeholder values used in CI and local dev before real CDK outputs are available.
# Override with actual CDK outputs at deploy time.
COGNITO_USER_POOL_ID=placeholder
COGNITO_CLIENT_ID=placeholder
COGNITO_REGION=us-east-1
API_BASE_URL=http://localhost:4000
